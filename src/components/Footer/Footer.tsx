import React, { FC } from "react";
import { FooterContainer } from "./Footer.styled";

const Footer: FC = () => {
  return <FooterContainer>Footer Section</FooterContainer>;
};

export default Footer;
